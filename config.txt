-- Configuração do GIT
git config --global user.name LauraChiarello
git config --global user.email laura_chiarello@live.com


-Criar o gitignore
dotnet new gitignore


--Criar projeto MVC
dotnet new mvc -o Pokedex

cd Pokedex

pacote necessário para manipular o banco de dados:
dotnet add package Microsoft.EntityFrameworkCore --version 9.0.8
dotnet add package Pomelo.EntityFrameworkCore.MySql --version 9.0.0

o que é a camada de contexto? criar herança. é a classe que vai representar o banco (fazer comunicação)